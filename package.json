{
  "name": "hello-server",
  "version": "1.0.0",
  "description": "Express server exposing GET /api/hello",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
